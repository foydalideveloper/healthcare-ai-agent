# Daily Report — April 24, 2026
## Healthcare AI Agent | Triple-H Co., Ltd.

### 1. Glasses watcher fixes
- Fixed silent-skip bug: `today_str` filename filter replaced with 48h mtime window
  (iOS names photos in UTC so "today" check dropped everything)
- Added HEIC decoding via `pillow-heif` (iPhone default format was being skipped)
- First-scan summary at startup — silent skips can't masquerade as "nothing happening"
- Removed duplicate `[WATCHING] inbox` log

### 2. Dashboard auth-gate recovery
- `require_current_user_id()` was blocking service-role backend RPC calls with 42501
- Added `auth.role() = 'service_role'` bypass — dashboard loads again

### 3. Drill-down logic audit — 9 bugs found, 7 fixed
- Exercise/Sleep at L4 (schema had no hour — now sleep uses sleep_start/end,
  exercise pins to first voice_report hour)
- Exercise double-count (45 min showing as 90) — watcher upsert with MAX
- Medicine was auto-filling prescription window (Vitamin D on every day Apr 16→24
  even without confirmation) — new `user_medication_intake` table, drill-down
  reads confirmed doses only
- Biometrics only surfaced HR — now composite `HR 72 · SpO2 98% · Glu 95 · T 36.6°C`
- Hydration ILIKE '%tea%' misclassified "tea sandwich" — word-boundary regex +
  food-suffix exclusion
- Goal-relative intensity via `std_lifestyle_plan` joined on user's age+gender

### 4. Gemma 4 evaluation (released Apr 3, 2026)
- Researched: E2B (2.3B), E4B (4.5B), 26B MoE, 31B dense; audio+video native on E2B/E4B
- Installed llama.cpp + Gemma 4 E2B-it GGUF on i3-12100 (CPU-only, ~6 tok/s)
- Built [benchmark_extractor.py](../../ml/inference/benchmark_extractor.py) —
  Gemma vs regex on 8 voice-report transcripts
- **Result: regex wins on both accuracy and speed** on this CPU setup
  - Food F1: regex 0.458 vs Gemma 0.375
  - Medication F1: regex 1.000 vs Gemma 0.875
  - Latency: regex 0.01s vs Gemma 44s (~4,400× faster)
- Verdict: keep regex as primary extractor; Gemma 4 E4B on GPU to be re-benchmarked
  once RTX 4070 SUPER frees from current fine-tune job

### 5. Migrations persisted
- `005_hrt_drilldown_logic_fixes.sql` — exercise/sleep/medicine/biometrics/hydration/auth
- `006_medication_intake_and_goal_relative_intensity.sql` — intake table + goal targets

### 6. Architecture decisions
- AIMB-G1 pipeline confirmed matches hand-drawn diagram (manual Cyan import is the gap)
- Mentra Live pipeline: 3 real options discussed
  - A) phone-only (light models, offline, lower accuracy)
  - B) phone relay + local PC (current-style, no OneDrive lag, one-user scale)
  - C) phone relay + cloud GPU (Modal/RunPod/Cloud Run, production-ready, 100K scale)
- Decision: Option C for production; 4070 SUPER stays as dev/fine-tune machine only

### Tomorrow
- Re-benchmark Gemma 4 E4B on 4070 SUPER once fine-tune job finishes
- Scaffold pill-event detection + phone push notification for confirmation
- Decide chatbot path: cloud Haiku API (Week 1 ship) vs on-device Gemma 4 E2B
