# Core
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-dotenv==1.1.0
pydantic==2.11.3
pydantic-settings==2.9.1

# HTTP client (Supabase REST API)
httpx==0.28.1

# Auth / Security
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4

# Data / ML (Phase 2+)
# torch==2.3.0
# numpy==2.2.0
# pandas==2.2.0

# Glasses pipeline — iPhone HEIC decoding (unpinned: 0.21 has no 3.14 wheel, 1.3 does)
pillow-heif>=0.21.0

# Testing
pytest==8.4.0
pytest-asyncio==1.0.0
